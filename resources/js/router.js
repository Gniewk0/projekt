import VueRouter from 'vue-router';


let routes = [
{
    // path: '/',
    // name: '',
    // component: '',
    // props: true,
}
]

export default new VueRouter({
    // mode: 'history',
    // routes
})
