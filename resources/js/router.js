import VueRouter from 'vue-router';


let routes = [
{
    // path: '/wnioski',
    // name: 'AppLayout',
    // component: AppLayout,
    // props: true,
}
]

export default new VueRouter({
    // mode: 'history',
    // routes
})
